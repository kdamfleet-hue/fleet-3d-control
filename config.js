// ===== Fleet 3D Control — Configuration =====
// عدّل هذه القيم حسب حاجتك. تُحمَّل قبل index.html وتضبط window.FLEET_CONFIG.
window.FLEET_CONFIG = {
  // مصدر بيانات المواقع من نفس نطاق تطبيق Flask لتفادي مشاكل CORS والنطاق القديم.
  // يتطلب الطلب جلسة دخول صالحة، وإلا تُستخدم البيانات التجريبية تلقائيًا.
  apiUrl: "/api/gps",

  // فترة تحديث المواقع بالمللي ثانية (3 ثوانٍ كما في الخطة)
  pollIntervalMs: 3000,

  // مركز العالم (الدمام) — تُسقَط الإحداثيات حوله
  center: { lat: 26.4207, lng: 50.0888 },

  // مقياس العالم: وحدات المشهد لكل متر حقيقي (كبّر/صغّر حسب انتشار الأسطول)
  worldScale: 0.02,

  // استخدام بيانات تجريبية متحركة عند تعذّر المصدر الحقيقي
  useMockOnError: true,
  mockVehicleCount: 8,

  // عتبات تلوين الخطر حسب السرعة (كم/س) — تُستخدم إن لم يوفّر المصدر حالة جاهزة
  speed: { warn: 90, danger: 120 },

  // مسارات الحركة (3D Routes): خط أثر يتلاشى خلف كل مركبة
  trail: { enabled: true, maxPoints: 80, sampleMs: 300 },

  // المناطق الحرجة (3D Zones): الإحداثيات بالدرجات، نصف القطر بالمتر
  zones: [
    { name: "منطقة حرجة — الميناء", lat: 26.4450, lng: 50.1050, radius: 1300, severity: "danger" },
    { name: "منطقة تحذير — الوسط",  lat: 26.4080, lng: 50.0700, radius: 1700, severity: "warn"   },
  ],
};
